{
  "name": "@frc-attendance/api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:bench": "tsx src/benchServer.ts",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply frc-attendance --local",
    "db:migrate": "wrangler d1 migrations apply frc-attendance",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@frc-attendance/shared": "0.1.0",
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@types/better-sqlite3": "^7.6.11",
    "typescript": "^5.5.4",
    "vitest": "^4.1.6",
    "wrangler": "^4.93.0"
  }
}
